if debug.info(1, "l") ~= 1 then
    game.Players.LocalPlayer:Kick("Safe dude..")
        while true do end
else
    print("Safe")
end

local functions = {print,warn,error,writefile,makefolder,rconsoleprint,setclipboard,rconsoleerr,rconsolewarn}
local hell_nah = {"githubusercontent","LRHUBEDIT"}
clonefunction = function() end
replacefunction = function() end
for i,v in pairs(functions) do
    local old
    old = hookfunction(v, function(...)
        local content = {...}
        for i2,v2 in pairs(content) do
            for i3,v3 in pairs(hell_nah) do
                if string.find(string.lower(tostring(i2)),string.lower(tostring(v3))) or string.find(string.lower(tostring(v2)),string.lower(tostring(v3))) then
                    print("Nigga ass dumb")
                    content = " So Let's raise the bar"
                    while true do end
                    return
                end
            end
        end
        return old(...)
    end)
end

local function CheckForSpy()
    local core = game:GetService("CoreGui")
    local keyword = "spy"
    
    for _, v in pairs(core:GetDescendants()) do
        if v:IsA("TextLabel") or v:IsA("TextButton") or v:IsA("TextBox") then
            if string.find(string.lower(v.Name), string.lower(keyword)) or string.find(string.lower(v.Text), string.lower(keyword)) then
                       while true do end
                end
            end
        end
end

CheckForSpy()

game:GetService("CoreGui").ChildAdded:Connect(function()
CheckForSpy()
end)

local allowedPlayers = {
   "shikybB",
    "iamgoingbegood",
    "Aistllx1",
    "alan11ago",
    "prodnivlac",
    "xaxadabest",
    "JAKETZY_YT"
}

local function isPlayerAllowed(player)
   local playerName = player.Name
   for i = 1, #allowedPlayers do
      if allowedPlayers[i] == playerName then
         return true
      end
   end
   return false
end

local player = game:GetService("Players").LocalPlayer
if not isPlayerAllowed(player) then
   player:Kick("You need whitelist baby")
else
local DrRayLibrary = loadstring(game:HttpGet("https://raw.githubusercontent.com/AZYsGithub/DrRay-UI-Library/main/DrRay.lua"))()

local window = DrRayLibrary:Load("Demonic HUB PLUS", "Default")

local tab = DrRayLibrary.newTab("Scripts", "13915106236")

tab.newButton("STARVING ARTIST", "Load A Script.!", function()
loadstring(game:HttpGet("https://raw.githubusercontent.com/SecretBaby1921/S.a/main/Protected_4208990344096930.lua.txt",true))()

end)
end
